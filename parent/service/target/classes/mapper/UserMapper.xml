<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuxi.mapper.UserMapper">
    <select id="getListUserDESC" resultType="com.zhuxi.pojo.VO.Admin.AdminUserVO">
        <![CDATA[
        SELECT
            user.id,
            user.name,
            user.phone,
            user.status,
            COUNT(CASE WHEN o.status = 3 THEN 1 END) AS orderCount,
            user.last_time
        FROM user JOIN `order` o ON  o.user_id =  user.id
        WHERE user.id < #{lastId}
        GROUP BY user.id, user.name, user.phone, user.status
        ORDER BY user.id DESC
        LIMIT #{pageSize}
        ]]>
    </select>


    <select id="getListUserASC" resultType="com.zhuxi.pojo.VO.Admin.AdminUserVO">
        SELECT
            id,
            display_name,
            avatar,
            phone,
            status,
            order_count,
            last_order_time
        FROM user_summary
        WHERE id > #{lastId}
        ORDER BY id ASC
        LIMIT #{pageSize}
    </select>


    <update id="updateUser" parameterType="com.zhuxi.pojo.DTO.User.UserUpdateDTO">
        UPDATE user
        <set>
            <if test="nickName != null">
                nickname = #{nickName},
            </if>
            <if test="customAvatarOss != null">
                custom_avatar_oss = #{customAvatarOss},
            </if>
        </set>
        WHERE id = #{id}
    </update>


</mapper>